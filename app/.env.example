# Copy this file to .env and fill in your values
# Do not commit .env to git (it's in .gitignore)

# Server port (default: 5000)
PORT=5000

# Vercel sets this automatically when deployed - no need to add locally
# VERCEL=1
