# Copy to .env and set your backend API URL (for production deploy).
# Local dev: frontend uses http://localhost:5000 if not set.

# Backend API base URL (no trailing slash). Leave empty for same-origin (e.g. when using proxy).
VITE_API_URL=
