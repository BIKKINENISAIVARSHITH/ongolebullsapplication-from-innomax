# Copy to .env and set your backend API URL (for production deploy).
# Local dev: frontend uses http://localhost:5001 if not set.

# Backend API base URL (no trailing slash). E.g. http://localhost:5001 for local backend on port 5001.
VITE_API_URL=
